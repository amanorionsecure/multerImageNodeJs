const { Router } =require("express");
// const travelCheckListController = require('../controller/travelCheckListController');

const router = Router();

// router.post('/list', travelCheckListController.travelCheckList);
// router.post('/add', travelCheckListController.travelCheckListAdd);
// router.post('/update', travelCheckListController.travelCheckListUpdate);
// router.post('/delete', travelCheckListController.travelCheckListDelete);

module.exports ={
    router
};